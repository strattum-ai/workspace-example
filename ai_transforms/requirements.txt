# Dependencies for the AI enrichment transforms in this folder.
# Installed by the platform when running them; install locally to lint/test.
strattum-sdk @ git+https://github.com/strattum-ai/strattum-sdk.git
pydantic>=2,<3
